#include"main.h"
#include<string.h>
/**
* _strlen_recursion - a function that returns the length of a string
* @s: a character string pointer
* Return: Length of the string
*/
int _strlen_recursion(char *s)
{
int len;
len = strlen(s);
return ((len));
}
