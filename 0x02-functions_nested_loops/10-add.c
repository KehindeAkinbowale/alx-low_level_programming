#include"main.h"
/**
* add(int, int)- prints the sum of two integers
* @x: number to be inputed into the function
* @y: number to be inputed into the function
* Return: 0(Success)
*/
int add(int x, int y)
{
int result;
result = x + y;
return (result);
}
