#include"main.h"
#include<string.h>
/**
* _strlen - a function that returns the length of a string.
* @s: string inputed into the function
* Return: 0(Success)
*/
int _strlen(char *s)
{
int l;
l = strlen(s);
return ((l));
}
