#include"main.h"
/**
* _isupper - A function that checks for uppercase alphabet character
* @c: character to be checked
* Return: 0(Success)
*/
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
